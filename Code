#include<stdio.h> 
#include<stdlib.h> 
#include<time.h> 

int main(){
    int number;
    srand(time(0));
    number =rand()%100 +1;//Generate a random number between 1-100
    //keep running the loop until the number is guessed
    printf("The number is %d \n",number);
    return 0;
}
